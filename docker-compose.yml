services:
  streamlit-app:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - .:/app
      - app_data:/app
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped

volumes:
  app_data:
